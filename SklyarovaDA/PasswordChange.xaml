<Window x:Class="SklyarovaDA.PasswordChange"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SklyarovaDA"
        mc:Ignorable="d"
                Title="PasswordChange" Height="350" Width="350">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="Green" Offset="0.0"/>
            <GradientStop Color="Yellow" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Смена пароля" 
       FontSize="20"
       FontWeight ="Bold"
       Foreground="#333"
       HorizontalAlignment="Center"
       FontFamily="Bahnschrift"
       Margin="0,10"
       Grid.Row="1"/>
        <TextBlock Text="Пароль"
            FontSize="14"
            FontWeight ="Bold"
            Foreground="#333"
            HorizontalAlignment="Center"
            FontFamily="Bahnschrift"
                   Margin="0,5"
            Grid.Row="2"/>
        <PasswordBox x:Name="txtPassword"
         Background="#FFFFC2"
         BorderBrush="#CCC"
         BorderThickness="2"
         Grid.Row="3"
                     Margin="0,5"
         Width="250"/>
        <TextBlock Text="Новый пароль"
                   FontSize="14"
                   FontWeight ="Bold"
                   Foreground="#333"
                   HorizontalAlignment="Center"
                   FontFamily="Bahnschrift"
                   Margin="0,5"
                   Grid.Row="4"/>
        <PasswordBox x:Name="NewPasswordBox"
                     Background="#FFFFC2"
                     BorderBrush="#CCC"
                     BorderThickness="2"
                     Grid.Row="5"
                     Margin="0,5"
                     Width="250"/>
        <TextBlock Text="Подтвердите новый пароль"
           FontSize="14"
           FontWeight ="Bold"
           Foreground="#333"
           HorizontalAlignment="Center"
           FontFamily="Bahnschrift"
                   Margin="0,5"
           Grid.Row="6"/>
        <PasswordBox x:Name="ConfirmPasswordBox"
             Height="20"
             Background="#FFFFC2"
             BorderBrush="#CCC"
             BorderThickness="2"
             Grid.Row="7"
                     Margin="0,5"
             Width="250"/>
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center" Height="90">
            <Button Content="Сменить" x:Name="SaveButton" Click="ChangeButton_Click" Margin="5,0" Height="30" Width="70"/>
            <Button Content="Отмена" x:Name="CancelButton" Click="CancelButton_Click" Margin="5,0" Height="30" Width="70"/>
        </StackPanel>
    </Grid>
</Window>

